### Apache Webserver Python Install Script

Installs an Apache webserver via apt within a python script.
Expects `Port` and `Rootpath` as parameters and Outputs the installed version of the webserver.
